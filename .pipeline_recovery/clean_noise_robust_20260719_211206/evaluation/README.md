# Evaluation

This package is the noiseless evaluation part of the repository. During the
transition, `python -m evaluation.run --gate diagonal` and
`python -m evaluation.run --gate nondiagonal` delegate to the existing root
`reproduce_numerics.py` script, so published checkpoints remain compatible.
